import type { VerificationMethod } from 'did-resolver';
interface LegacyVerificationMethod extends VerificationMethod {
    publicKeyPem?: string;
    publicKeyBase64?: string;
}
export declare function verifyRS256(data: string, signature: string, authenticators: LegacyVerificationMethod[]): LegacyVerificationMethod;
declare type Verifier = (data: string, signature: string, authenticators: LegacyVerificationMethod[]) => LegacyVerificationMethod;
declare function VerifierAlgorithm(alg: string): Verifier;
export default VerifierAlgorithm;
